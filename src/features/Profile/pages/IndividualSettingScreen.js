import React from 'react';
import {View, StyleSheet, Button} from 'react-native';

const IndividualSettingScreen = () => {
    return (
        <View>
            
        </View>
    );
}

const styles = StyleSheet.create({
    header: {
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'space-between'
    }
})

export default IndividualSettingScreen;
